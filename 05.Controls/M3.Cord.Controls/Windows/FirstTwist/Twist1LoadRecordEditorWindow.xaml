<Window x:Class="M3.Cord.Windows.Twist1LoadRecordEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:NLib.Wpf.Controls;assembly=NLib.Wpf.Controls"
        xmlns:page="clr-namespace:NLib.Wpf.Pages;assembly=NLib.Wpf.Controls"
        mc:Ignorable="d"
        Title="Load Yarn"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Height="800"
        Width="1100">
    <page:NPage HeaderText="Load Yarn">
        <DockPanel LastChildFill="True" Margin="5">
            <!--  BUTTON BARS  -->
            <DockPanel LastChildFill="True"
                       DockPanel.Dock="Bottom"
                       Height="40">
                <ctrl:FontAwesomeButton x:Name="cmdCancel"
                                        DockPanel.Dock="Left"
                                        IconType="Cancel"
                                        Text="Cancel"
                                        Click="cmdCancel_Click" />
                <ctrl:FontAwesomeButton x:Name="cmdOk"
                                        DockPanel.Dock="Right"
                                        IconType="Ok"
                                        Text="Ok"
                                        Click="cmdOk_Click" />
                <Grid />
            </DockPanel>
            <!--  WORK AREA  -->
            <DockPanel LastChildFill="True">
                <DockPanel DockPanel.Dock="Top">
                    <!--  DATE TIME  -->
                    <ctrl:NTextBox CaptionText="DATE/TIME:"
                                   DockPanel.Dock="Left"
                                   Text="{Binding Path=ProductionDate, StringFormat='{}{0:dd/MM/yyyy HH:mm:ss}'}"
                                   IsReadOnly="True"
                                   Width="250" />
                    <!--  ITEM YARN  -->
                    <ctrl:NTextBox CaptionText="Item Yarn:"
                                   DockPanel.Dock="Left"
                                   Text="{Binding ItemYarn}"
                                   IsReadOnly="True"
                                   Width="300" />
                    <!--  LOT NO  -->
                    <ctrl:NTextBox CaptionText="Lot No:"
                                   DockPanel.Dock="Left"
                                   Text="{Binding ProductLotNo}"
                                   IsReadOnly="True"
                                   Width="150" />
                    <CheckBox x:Name="chkTest" Style="{StaticResource CommonCheckBox}" 
                              Margin="0,0,0,10"
                              FontSize="20"
                              Content="Test"
                              VerticalAlignment="Bottom"
                              Checked="chkTest_Checked"
                              Unchecked="chkTest_Unchecked"/>
                    <!--  Doff No  -->
                    <ctrl:NTextBox CaptionText="Doff No:"
                                   DockPanel.Dock="Left"
                                   Text="{Binding DoffNo}"
                                   IsReadOnly="True"
                                   Width="150" />
                    <!--  Shift  -->
                    <ctrl:NTextBox CaptionText="Shift:"
                                   DockPanel.Dock="Left"
                                   Text="{Binding ShiftName}"
                                   Width="100" />
                    <Grid />
                </DockPanel>
                <DockPanel DockPanel.Dock="Top">
                    <ctrl:NTextBox x:Name="txtScanPallet"
                                   CaptionText="Pallet No:"
                                   DockPanel.Dock="Left"
                                   Text=""
                                   Width="200"
                                   PreviewKeyDown="txtScanPallet_PreviewKeyDown" />
                    <Grid />
                </DockPanel>
                <DockPanel DockPanel.Dock="Top">
                    <ctrl:NTextBox x:Name="txtPallet"
                                   CaptionText="Pallet No:"
                                   DockPanel.Dock="Left"
                                   Text=""
                                   Width="200" />
                    <ctrl:NTextBox x:Name="txtTraceNo"
                                   CaptionText="Trace No:"
                                   DockPanel.Dock="Left"
                                   Text=""
                                   IsReadOnly="True"
                                   Width="200" />
                    <ctrl:NTextBox x:Name="txtTotalCH"
                                   CaptionText="Total CH:"
                                   DockPanel.Dock="Left"
                                   Text=""
                                   IsReadOnly="True"
                                   Width="180" />
                    <ctrl:NTextBox x:Name="txtTotalWg"
                                   CaptionText="Total Weight:"
                                   DockPanel.Dock="Left"
                                   Text=""
                                   IsReadOnly="True"
                                   Width="180" />
                    <Grid />
                </DockPanel>
                <DockPanel DockPanel.Dock="Top">
                    <ctrl:NTextBox x:Name="txtSPNo"
                                   CaptionText="SP No:"
                                   DockPanel.Dock="Left"
                                   Text="1"
                                   Width="200" />
                    <ctrl:NTextBox x:Name="txtDeckNo"
                                   CaptionText="เส้นที่:"
                                   DockPanel.Dock="Left"
                                   Text="1"
                                   Width="200" />
                    <ctrl:NTextBox x:Name="txtYarnBarcode"
                                   CaptionText="Yarn Barcode:"
                                   DockPanel.Dock="Left"
                                   Text=""
                                   Width="200" />
                    <Button x:Name="Add"
                            VerticalAlignment="Bottom"
                            Style="{StaticResource InlineButton}"
                            Content="Add"
                            Click="Add_Click" />
                    <Grid />
                </DockPanel>
                <!--  SPs  -->
                <ListView x:Name="grid"
                          Margin="0,0,0,5"
                          FontSize="{StaticResource FontSize18}"
                          ItemContainerStyle="{StaticResource BASEListViewItemContainer}"
                          ItemsSource="{Binding}"
                          Style="{StaticResource SingleSelectListView}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Trace No" CellTemplate="{StaticResource TraceNo}" />
                            <GridViewColumn Header="Doff No" CellTemplate="{StaticResource DoffNos}" />
                            <GridViewColumn Header="SP No" CellTemplate="{StaticResource SPNos}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </DockPanel>
        </DockPanel>
    </page:NPage>
</Window>

